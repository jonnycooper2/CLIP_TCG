# Core ML & Search
torch --index-url https://download.pytorch.org/whl/cpu # Force CPU version
faiss-cpu # CPU version of FAISS
open_clip_torch
numpy
Pillow

# Web Framework & Server
fastapi
uvicorn[standard] # Includes standard dependencies like httptools

# HTTP Requests (for fetching image URL)
requests
